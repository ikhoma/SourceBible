---
id: bug-006
type: defect
status: by-design
severity: medium
priority: P2
area: reader/original-localization
component:
reporters: [oleksiukviacheslav]
app_version: "1.0"
device: iPhone 11
ios_version: "26.5"
context: RST translation; Gen 1:11, Verse → Original; "any book"
date_reported: 2026-06-25
date_resolved: 2026-07-14
duplicate_of:
related: [bug-005]
source: "Drive doc 1juQYk66MMAjj5iUWYu0r7OCtuR1tCekGc7p6FzG1bzY — BUG REPORT 20"
---

# Description

With RST active, the Verse → Original view shows the original-language gloss/text
in English instead of matching the RST translation. Reporter says it happens on
any book.

## Steps to Reproduce

1. Switch translation to RST.
2. Open Gen 1:11 → Verse → Original.

## Expected

Original-tab content reflects the RST translation rather than English.

## Actual

Original text/gloss shown in EN.

## Screenshots

- Drive mailbox: `img/33.jpg`

## Notes

Tester's own ref: "BUG REPORT 20". iPhone 11 / iOS 26.5.
TRIAGE FLAG: may overlap the "lexicon/original glosses are English for MVP"
strategy — confirm whether this is the per-word gloss (by-design EN) or the verse
text itself (should follow RST). See [bug-005].

## Investigation (2026-06-26 · subagent, opus)

Mostly **by design**: the Original tab shows `word.gloss` = English Macula gloss
(`DatabaseService.loadWords` COALESCE of `gloss_display`/`gloss_macula`/`gloss`, ~219–221);
there is no RST gloss column, so English is expected for MVP. Section & morphology labels
DO localize (same `xcstrings` path as bug-005).

## Proposed fix

No code fix for the gloss (deferred to the translated-lexicon stage). Recommend
**reclassify** as by-design + add a known-design note (like the page-turn PDR) so repeat
"original is in English" reports dedup. If the reporter meant the labels, that's the
bug-005 xcstrings gap. Confidence: medium (gloss vs labels ambiguous).

## Verdict — BY DESIGN (2026-07-14)

Recorded as **PDR-Lexicon-Language** (`docs/product decisions/`).

The Original tab renders `word.gloss` — the **English Macula gloss**
(`DatabaseService.loadWords`, COALESCE of `gloss_display` / `gloss_macula` / `gloss`,
~219–221). There is no RST/UA gloss column in the DB, so English is the expected MVP
output regardless of the active translation. A translated lexicon is a later stage
(ADR-006); machine-translating lexical definitions is explicitly rejected.

**Split, though — the report had a real half:** section and morphology **labels** DO have
to localize, and they weren't, because `String(localized:)` bypasses the `LocalizedBundle`
swizzle. That was a genuine defect and is fixed (bug-005 / bug-018, 2026-07-13).

- Gloss / definition text in English → **by design**, this ticket.
- Labels in English → **was a bug**, fixed.

Registered in `docs/ux/known-design-friction.md` with an **Accept** decision so repeat
reports dedup instead of being re-investigated.
