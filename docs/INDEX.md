# SourceBible — docs index

One line per document. Read this file at the start of every session.
Load the full document only when the task is relevant to it.

---

## Architecture Decisions (`architecture/`)

| File | Title | Status | Summary |
|---|---|---|---|
| `ADR-001-platform-stack.md` | Platform Stack | Accepted (amended 2026-05-26, 2026-07-31) | SwiftUI native; iOS 18 minimum target (iOS 26 features behind #available); Swift 6 strict concurrency; ~~compatibility sprint deferred~~ → **target реально опущено до 18.0 2026-07-31** (гілка `ios18-target-flip`, plan-ios18-compat Phase B); заявлений мінімум і фактичний тепер збігаються |
| `ADR-003-file-modularity.md` | File Modularity | Accepted | Split large Swift files into focused modules; FlowLayout/CapsuleNavGroupStyle extracted |
| `ADR-005-highlights-bookmarks-notes.md` | Highlights, Bookmarks, Notes Architecture | Accepted (with amendments) | Cross-tab navigation via AppNavigationRouter; GRDB store; UserDataStoreProtocol |
| `ADR-006-localization-translation-provider.md` | Localization: TranslationProvider & Language Switch | Accepted | MorphologyDecoder emits MorphKey constants; BundleTranslationProvider for MVP; Bundle.main swizzled via object_setClass for instant switch; upgrade path → DB → Remote \| **Amendment 2026-08-03:** `AppLanguage.resolved` / `.narrowed(_:)` — одна резолюція мови замість п'яти різних дефолтів. … → **[повний запис](INDEX-amendments.md#adr-006-localization-translation-provider)** |
| `ADR-007-localization-completion.md` | Localization Completion: Book Names, Model Data, xcstrings Gaps | Implemented (QA pending) | BibleBookNames dual-lang done; Theologian static var fixed 2026-05-26; QA pass still open |
| `ADR-008-search-architecture.md` | Search Architecture MVP | Accepted (amended 2026-06-11, 2026-07-07, 2026-08-06, 2026-08-07) | FTS5 text search; predictive via search_terms table; vector search deferred to V1.5 (ONNX + sqlite-vec). Amend 2026-06-11: suggestions rendered inline (Apple Music pattern), `.searchSuggestions` dropped. Amend 2026-07-07: LIMIT 150 → infinite scroll (SQL LIMIT/OFFSET + COUNT, rowid tiebreak) … → **[повний запис](INDEX-amendments.md#adr-008-search-architecture)** |
| `ADR-009-repository-layer.md` | Repository Layer | Superseded | BibleRepository never built; HighlightRepository superseded by ADR-012; DatabaseService used directly |
| `ADR-010-bottomsheet-file-split.md` | BottomSheet File Split | Accepted | Modular split of bottom sheet components — implemented |
| `ADR-011-notes-bookmarks-persistence.md` | Notes & Bookmarks Persistence | Superseded by ADR-012 | UserDefaults approach rejected; kept as historical record |
| `ADR-012-unified-user-data-layer.md` | Unified User Data Layer | Accepted (amended ×4) | GRDB writable DB; sync-ready schema; block-based notes; PreAuthIdentity pattern |
| `ADR-013-word-usage-book-groups.md` | Word Usage Tab — Per-Book Concordance Data Model | Accepted | Replace flat concordance list with BookUsageGroup per-book breakdown; N per-book queries on MainActor; chapter*1000+verse encoding; forward-compat with v1.5 rhetorical_weight |
| `ADR-014-verse-text-view-cache-invalidation.md` | VerseTextView Cache Invalidation via `.id()` | Accepted | Use SwiftUI `.id()` keyed on `verseId+translationId+highlightColor` to force UITextView recreation on content changes; preserves coordinator cache for selection fast path |
| `ADR-015-trailing-chars.md` | Trailing Characters from Macula `after` Attribute | **Accepted** (реалізовано 2026-06) | Store `after_char TEXT` in word table from XML; `BibleWord.displayText` for rendering; migration script for existing DBs; fixes maqaf ־ missing from Original tab |
| `ADR-016-original-pill-nasb-bridge.md` | Original Pill — NASB-Gated Clickability, Unified Word Page, Verse Highlight | Accepted (amended 2026-06-22) | Amend 2026-06-22: clickability gate switched NASB→per-displayed-translation via one canonical word↔segment mapping (translation order, occurrence-indexed); fixes nav-order + repeated-word jump; NASB-set/morph gate retired (measurement: no coverage advantage); Phase 2 = unify tagging across all modules (verse_markup) |
| `ADR-017-book-covers.md` | Book Covers — Architecture | Accepted | `BookCoverView` + `BookCoverData`; Doré engraving on all 66 books; blue gradient bg; `hideBookCovers` Settings toggle; minimal changes to ReaderView |
| `ADR-018-translation-native-book-names.md` | Translation-Native Book Names and Order | Accepted | `book_name` table (book_id, translation_id, long_name, short_name, sort_order); extracted from MyBible `books` table in build_db.py; BibleBookNames becomes UI-only fallback; enables per-translation ordering. Живить пошук: канонічний порядок видачі через `COALESCE(book_name.sort_order, book.num-1)` (ADR-008 amend 2026-08-07) |
| `ADR-019-strongs-original-lemma-source.md` | Source of `strongs.original` — TBESH vs Macula `word.lemma` | Accepted | Keep TBESH as primary headword source; improve Macula fallback in `_enrich_strongs_stubs` to use frequency-ranked `GROUP BY lemma ORDER BY COUNT(*) DESC` instead of `LIMIT 1`; 11% of sub-entries have multiple lemma values in Macula (H871a: 3 variants, H3807a: 6 variants) |
| `ADR-020-hebrew-translit-build-validation.md` | Hebrew Transliteration Build-Time Validation | **Accepted** (реалізовано 2026-06) | Two-level validation in `_apply_bh_hebrew_translit()`: per-verse count parity + per-slot Strong's alignment; writes `data/hebrew_translit_mismatches.tsv`; fixes 3 gaps in rebuild plan (unverified HTML regex, narrow translit char class, missing `:count` sentinel) |
| `ADR-021-study-mode-scroll-positioning.md` | Study Mode — Verse Pinning & Sheet Sizing | Accepted (covers-off + covers-on done) · **amended 2026-08-18** (bug-031: `detentTopOffset` НЕ одне число — 16 на iOS 26 / 34 на iOS 18, заміряно; поправка стала в межах платформи, тож із safe area її виводити не треба; додано рантайм-калібрування з persist + прив'язкою до версії ОС, бо на SE Δ інша. Побічно знайдено дефект: на iOS 18 sheet НАКРИВАВ картку на −10 pt. Рівні 20 pt зазори — **свідомо не робимо**, потребують непрозорого підкладу, а не чисел) | Decision trail for Study-Mode scroll: atomic `StudyPinView` (verse's own background reads frame+offset together — no cross-layer skew) replaces scrollTo; NO top inset (content above gives headroom) — kills the entry jerk AND covers-on rubber band … → **[повний запис](INDEX-amendments.md#adr-021-study-mode-scroll-positioning)** |
| `ADR-022-analytics-event-collection-strategy.md` | Analytics Event-Collection Strategy | Accepted | Adoption = `feature_adopted_<feature>` once per session, **6 granular features 1:1 with UI surfaces** (original/lexicon/concordance/commentary/cross_reference/parallel_translation — original≠lexicon≠concordance, see glossary); first three form word-study funnel … → **[повний запис](INDEX-amendments.md#adr-022-analytics-event-collection-strategy)** |
| `ADR-024-cross-reference-back-stack.md` | Cross-Reference Back-Stack Navigation | **Accepted** (реалізовано; `crossRefBackStack` у ReaderViewModel) | Cross-ref переходи у Study Mode тепер мають back-стек (`crossRefBackStack` у ReaderViewModel); leading toolbar морфиться між 3 станами — пікер/«Закрити»(стек порожній)/«‹ Назад»(крок по cross-ref історії); `‹ ›` чеврони (intra-chapter) стек НЕ чіпають … → **[повний запис](INDEX-amendments.md#adr-024-cross-reference-back-stack)** |
| `ADR-025-app-preferences-storage.md` | App Preferences Storage Layer | Accepted | Lightweight typed facade over `UserDefaults` (NOT a new DB, keeps `@AppStorage`): `AppStorageKeys` = single source of truth for all key names (kills `"appLanguage"`×4, `"analyticsEnabled"`×4 duplication + typo risk); keys tagged by lifecycle category (appearance / ephemeral reading+search / launch pref / consent / migration); `AppPreferences` exposes SCOPED clears (`clearReadingHistory`, `resetAppearanceToDefaults`) — consent & migration flags NEVER wiped by design (blanket reset banned). New `Services/AppPreferences.swift`; refactors all call-sites; `ReadingPositionStore` (resume-position spec) uses these keys. **Amend 2026-07-31:** друга launch-преференція `translationLaunchBehavior` (дефолт `fixed` = стара поведінка) + ефемерний `lastUsedTranslationId`; `defaultTranslationId` НАВМИСНО лишається в appearance/preference (явний вибір переживає clear history), `lastUsedTranslationId` — у ephemeral (чиститься разом із позицією) | **Amendment 2026-08-03:** дефолт `translationLaunchBehavior` `fixed` → **`lastUsed`** (аргумент «не міняти мовчки для наявних» відпав — база = тестери); + крок 3 у ланцюгу фолбеків стартового перекладу: `AppLanguage.defaultTranslationId` (UK → `UBIO`, інакше `KJV`), бо раніше україномовний на першому запуску бачив KJV. `AppLanguage.resolved` — спільна резолюція мови для сцени й стора.
| `ADR-023-status-bar-style-control.md` | Status Bar Style Control in SwiftUI App Lifecycle | Reverted (backed out 2026-06-21) | Білий статус-бар на обкладинках. SwiftUI App lifecycle не має API для статус-бара. Поточно: підміна `window.rootViewController` контейнером — **зламало `.preferredColorScheme`** (dark mode), бо HC перестав бути коренем; довелось перебрати appearance на `overrideUserInterfaceStyle`. Research: … → **[повний запис](INDEX-amendments.md#adr-023-status-bar-style-control)** |
| `ADR-026-reader-chapter-paging-tabview.md` | Reader Chapter Paging (TabView → UIPageViewController) | **IMPLEMENTED 2026-07-10** (iOS 26; iOS 18 = legacy fallback до Phase B) · **amended 2026-08-17** — пейджинг перетинає межу книги (ux-020): сторінка = `ChapterRef` за наскрізним номером глави в каноні, безшовно, кінці канону глухі | Native slide on chevron AND **full-surface** swipe (closes ux-001; PDR-Page-Turn-Gesture-Zone gate passed on device). **TabView(.page) tried & REJECTED empirically**: clips pages to safe area → вбиває cover bleed (ADR-017) і Liquid Glass scroll-edge блюр тулбара … → **[повний запис](INDEX-amendments.md#adr-026-reader-chapter-paging-tabview)** |
| `ADR-029-ohienko-translation.md` | Інтеграція перекладу Огієнка (UBIO'88) | **Accepted** (ліцензійний блокер знято 2026-07-31) | ✅ **Ліцензія підтверджена 2026-07-31: CC BY-SA 3.0** за прямим дозволом УБТ/UBS на видання Огієнка **до 1991**, включно з ювілейним **1988** (= наш UBIO'88); джерело — blog.wikimedia.org.ua, 2013-11-27. … → **[повний запис](INDEX-amendments.md#adr-029-ohienko-translation)** |
| `ADR-030-strongs-root-concordance.md` | Кореневий конкорданс через Strong's-деривацію | **Accepted** | Групування за коренем не кодує жодне джерело (strongs/word/TBESH/BSB/Macula — перевірено 2026-07-20); Macula coredomain = за значенням, не коренем (зливає r-ch-m+ch-n-n у CORE:036, розсипається на nasa H5375). Рішення: … → **[повний запис](INDEX-amendments.md#adr-030-strongs-root-concordance)** |
| `ADR-028-versification.md` | Версифікація — курований довідник замість евристики | **Accepted** — фази 1 і 2 реалізовані (заміряно 2026-08-07: `verse_org` = 155 621, `verse_map` видалена); **amended 2026-08-16** (bug-036: паралельні переклади — четвертий споживач, пропущений фазою 2, бо не читав `verse_map`; правило «крос-трансляційна поверхня → через `verse_org`» + гейт `verify_parallel_alignment.py` у `rebuild.sh`) | **Заміряно: 4 152 з 7 292 рядків `verse_map` хибні (57%)** — «Оригінал» показує іврит чужого вірша. RST Псалтир зламаний майже цілком (2 508 віршів: синод. Пс N = євр. N+1, зсув на ГЛАВУ). KJV 1 Chr 6:5 показує MT 6:36 замість 5:31. Три дефекти: `align_chapter()` шукає лише в межах глави … → **[повний запис](INDEX-amendments.md#adr-028-versification)** |
| `ADR-027-modular-commentary-modules.md` | Модульні commentary-модулі з OTA-оновленням | Proposed — **чотири відкриті питання закрито 2026-08-28** (архітектурна сесія, без коду) | Винести коментарі з моноліту `sourcebible.db` в окремі read-only SQLite-модулі (1 файл = твір), оновлювані OTA незалежно від релізу App Store; одиниця оновлення = книга; ядро (Macula/verse/strongs) лишається монолітом. Рішення 2026-08-28: власна канонічна схема + MyBible-імпортер (book_number нестабільний, немає org-anchor/provenance у MyBible-схемі); FTS5 = власна таблиця на модуль + UNION ALL по ATTACH (перевірено спайком; content= не може бути cross-db); sub-book дельти відкладено з тригером ~5–10 МБ на найбільший файл автор×книга; підпис = checksum MVP, Ed25519 fast-follow за появи sideload. Привід: діри в джерелах (Calvin Isaiah 49–66 нема в SWORD … → **[повний запис](INDEX-amendments.md#adr-027-modular-commentary-modules)** |
| `ADR-031-notifications.md` | Локальні нотіфікації (retention, local-first) | **Proposed** (amended 2026-07-31) | Local-first push; APNs → Фаза 2 (з ADR-027), зараз лише `UNUserNotificationCenter`. Два канали: **A «Продовжити дослідження»** (event-driven, ядро=word-study, fallback позиція→нотатка/`crossRefBackStack`; **нескінченний двигун steady-state retention** … → **[повний запис](INDEX-amendments.md#adr-031-notifications)** |
| `ADR-032-haptic-feedback-map.md` | Карта хаптики — семантичні методи замість сил вібрації | **Accepted** (2026-08-02) | Один тип `Haptics` (`Views/Components/`), три семантичні методи: `selectionChanged()` (selection — тап по слову, чеврони вірш/слово, зняття highlight), `lightTransition()` (impact `.light` … → **[повний запис](INDEX-amendments.md#adr-032-haptic-feedback-map)** |
| `ADR-034-markup-stripping-single-source.md` | Зняття розмітки вірша — одне джерело правди | **Accepted** (реалізовано 2026-08-07; кроки 6–7 відкриті) | Правила `<S>/<f>/<n>/<i>` реалізовані ТРИЧІ: `build_db.py::_search_text` (build-time → `text_clean`), `String+BibleMarkupCore` (runtime, 4 поверхні), `VerseParser` (токенайзер, рідер). Заміряно: … → **[повний запис](INDEX-amendments.md#adr-034-markup-stripping-single-source)** |
| `ADR-033-lexicon-stem-ranking.md` | Лексикон: порода цієї форми, а не діапазон кореня | **Accepted** (2026-08-03) | BDB-стаття івритського дієслова двошарова: `1)` = склеєний заголовок кореня, `1a)/1b)` = породи. Рендерились у порядку джерела, тож першим ішов заголовок. Ос. … → **[повний запис](INDEX-amendments.md#adr-033-lexicon-stem-ranking)** |
| `ADR-035-guided-first-study-onboarding.md` | Guided First Study — онбординг через одне слово (חֶסֶד / Вих 34:6) | **Proposed** (2026-08-18) | Онбординг = **одне кероване дослідження слова на реальному вірші**, НЕ тур по UI. Привід: немає ані first-run (ux-005), ані старт-екрана (ux-016) → уся цінність за нерозказаним жестом тапу. **Критерій вибору слова (генералізований):** інструмент має бути *необхідним* — слово без однослівного аналога, потребує контексту, переклади видимо розходяться, частотність дає форму в конкордансі, не доктринально спірне → **hesed H2617**. **Вірш Вих 34:6** — самоопис Бога + **кластер** неперекладних (rachum/channun/erek appayim/emet) → фінал онбордингу = **передача керма** («ще 4 такі слова»), перший самостійний тап гарантовано вдалий. **Регістр: подив, НЕ суперечка** — свідома відмінність від `deep_dive` ADR-031 (там гачок = спір). **Маршрут = воронка ADR-022** (parallel→original→lexicon→concordance→xref) ⇒ **нуль нової інструментації** (+3 події onboarding_*). **Мовна асиметрія як рішення:** EN (первинний сегмент) — на **реальному механізмі** (клікабельність ADR-016, ✅ підтверджено на пристрої 2026-08-18), нуль курації; UK — курована ручна розмітка **одного вірша** (~17 токенів word↔Strong's), бо Огієнко без Strong's-тегів — це **розмітка, а не права** (ліцензія ADR-029 чинна) + ручні укр. глоси (крок «лексикон» = кульмінація; людський текст ≠ MT, trust-правило ціле). Дані: bundled JSON (клас ассету ADR-031, не в БД). Подача: реальний UI + накладка (тонко налаштований UI НЕ переписувати), як контент а не туторіал, skippable + повтор з Меню. **Заміряно Mixpanel 90д (n=9, самодобірні волонтери):** 9→7→6→5 (78/86/83%) — жест виявляється без інструктажу, але холодна установка = сліпа пляма, яку пул структурно не закриє; тижневий ряд confounded (життєві обставини) — не сигнал. Ризик **«підміни»** (курований острівець обіцяє більше за застосунок) → мітигації: EN на реальному, чесне обрамлення фіналу, колекція 3–5 місць як місток (P1). ⚠️ **ЗНАЙДЕНО 2026-08-18: омонім H2617** — конкорданс показує Лев 20:17 «wicked thing» (BDB חֶסֶד II «сором/ганьба», не «вірна любов»; так само Прип 14:34); Strong's зливає, TBESH/dStrong розводить суфіксом (H2617A/B) → перевірити, чи запит **зрізає суфікс** до базового номера — тоді це **класовий** дефект, як заборонений у CLAUDE.md для xlit (H871a≠H871), але в конкордансі; **блокує крок 5 онбордингу**. ⛔ Перед кодом перевірити на Mac: Macula-розмітка Вих 34:6, НЕпорожні крос-рефи + коментар, конкорданс H2617; запасні слова racham H7355 / emet H571. Наступне: перевірка бази → `spec-guided-first-study.md` |
| `ADR-036-ukrainian-gloss-layer.md` | Український шар глос — наскрізні одиниці перекладу | Proposed | Вкладка «Оригінал» показує українцям англійські глоси. Рішення: шар **одиниць**, не входжень — 305 349 слотів зводяться до **78 451** унікальних одиниць, з виведеним відмінком **94 743** (+20.8%, повторюваність падає лише 82.9%→78.3%). Ключ = (Strong:morph)+контекстна глоса+відмінок, `unit_key BLOB(16)` **колонкою в `word`** (зміна схеми — поточний scope її DENY). Відмінок: грецька безкоштовно (`case` уже в `morph`), іврит із `frame` (94.7% дієслів) + `state`, але **50.6% іменникових слотів лишаються без тега** → оракул = два укр. переклади (Огієнко ADR-029 + Громов), вирівнювання **заморожене TSV у репо** (вхід у білд, не крок білду). Доставка: **все в бандлі** — модуль 4.34 МБ = 0.15% ліміту; ODR застаріле з iOS 27. Один beta-бейдж на весь шар до 100% вичитки. Знято: `unit_book`, покнижне оновлення, блокуючий гейт на JOIN (→ `EXPLAIN QUERY PLAN`) |
| `ADR-037-commentary-selection-share-note.md` | Commentary Text Selection — Share & Add-to-Note with Attribution | Proposed | Довгий тап у коментарі (Calvin/Henry/Spurgeon/Owen) вмикає нативне виділення тексту. **Не** SwiftUI `.textSelection` (немає гачка ні на буфер обміну, ні на кастомні пункти) і **не** `buildMenu(with:)` (той задокументований для main/context menu systems, НЕ для edit menu виділення) — правильний хук це `UITextViewDelegate.textView(_:editMenuForTextIn:suggestedActions:)`, iOS 16+, ⚠️ availability обривається на 27.0 (депрекація в iOS 27 → тримати Option C запасним). Два власні пункти: «Поділитися» (замінює голий системний Share на атрибутований `"quote" — Theologian, Book ch:verse`; системний прибираємо фільтром `suggestedActions`, не `builder.remove`) і «Додати в нотатку» (перший UI для дормантного `NoteBlockType.quote`/`QuoteBlockContent`, ADR-012). Copy лишається ванільним. Атрибуція завжди з координат секції, не з парсингу виділення. **Рендер: один СКРОЛЯЩИЙ `UITextView` на всю площу sheet-а**, шапка богослова виходить зі скролу — бо заміряно (2026-08-27, read-only БД): найдовша секція не Owen 231k, а **Calvin Ps 150:6 = 329 343 символи / 3 293 абзаци**, 34 секції >100k; нескролящий `UITextView` у SwiftUI `ScrollView` = синхронна верстка всього документа + в'юшка на 150k+ pt + вкладені скроли під час виділення → відкинуто. ⚠️ **Знайдено ревʼю:** `.quote` ніде не рендериться (`NoteEditorView`/`NoteCardView` фільтрують лише `.verse`+`.text`) — рендер має йти в тому ж коміті, інакше кнопка мертва. Свідомо поза обсягом: багата розмітка коментарів і курація джерел (`data/New/`, дублі Calvin, «хвостові» мегасекції на останньому вірші книги) — окремі майбутні ADR |
| `ADR-038-donation-prompt.md` | Донат-промпт: тригер, показ, rate-limit | Proposed | Продовжує ADR-031 (`pos-001`, in-app, поза push): банер — MSS-тригер поки застосунок активний. **Чотири входи в `DonationView`** (Menu нейтральний / About mission-пітч / Word-tab функціонально-специфічний / новий MSS-тригерований `CommunityView` — About+CommunityView дублюють пітч, обʼєднання свідомо відкладено). Локальний `isMeaningfulSessionReached` proxy в `SessionTracker` (тривалість ≥60с І глибина-сигнал ≥1) — окремий, грубіший за тюнюваний Mixpanel-MSS, бо `flush()` спрацьовує вже на backgrounding, коли UI показати нема на чому. **Amendment 2026-09-01:** глибина-сигнал звужено до `wordNavCount > 0 OR featureCounts non-empty` (без `annotationsCreated` — хайлайт/нотатка/закладка є в кожному біблійному застосунку, не свідчить про розуміння відмінності SourceBible; `wordNavCount` лишається, бо тягне `loadStrongs(for:)`), скасовує попереднє «не звужувати» від 2026-08-31. Момент показу — вихід зі Study Mode (`activeSheet: .verse → nil`, не таймер бездіяльності: тихе читання не інкрементує жоден лічильник, false-positive). Rate-limit — власний прапорець (не спільний `InAppMessageCenter`-примітив, відкладений окремою чернеткою): AND-каденс ≥3 MSS І ≥7 днів перший показ, ≥5 MSS І ≥30 днів наступні, стеля 3/365 (як Apple `SKStoreReviewController`), `hasDonatedEver` — постійне глушіння. Гонка primary/fallback-тригерів закрита сесійним стейт-машином `DonationPitchState` (`.idle/.armed/.presented`) у `ContentView`. Companion: `spec-donation-community.md`. |
| `ADR-039-update-availability-toasts.md` | Toast-повідомлення про доступні оновлення (застосунок + commentary-модулі) | Proposed | Два ad-hoc launch/foreground-тригери з **різним часом активації** (амендовано 2026-08-31): app-update — через Apple `itunes.apple.com/lookup?bundleId=...` (не App Store Connect API, і не власний `app_version.json` — той план відкинуто, бо S3/R2-інфраструктура ADR-027 сама планована на наступні версії; Apple-джерело не залежить від неї й не потребує бандленого App Store ID, `trackViewUrl` з відповіді дає й перевірку версії, і посилання для тапу) — **можна реалізувати вже в поточному циклі**. Module-update — звірка `installed_modules` проти `manifest.json` ADR-027 — лишається заблокованою на нереалізованому ADR-027 §OTA. UI — toast, не alert/sheet/banner: новий generic `Views/Components/ToastView.swift`; спільний тригер (launch/foreground, throttled) для обох, але мережевий шар більше не спільний клієнт (різні хости/схеми), dedup-стан окремий (rate-limit прапорці за патерном ADR-025, `InAppMessageCenter` знову розглянутий і відкинутий, як в ADR-038 — консолідацію з донат-банером свідомо не робимо). Черга з макс. 2 елементів (app-update першим); показ чекає закриття будь-якого активного sheet (Study Mode, `CommunityView` ADR-038) — `.sheet` рендериться шаром над `ContentView`-оверлеєм, тож "поверх" технічно не працює тривіально. Dedup на рівні версії, не сесії. Тап: app-update → App Store (`trackViewUrl`); module-update → новий read-only Menu-екран «Оновлення» (§8, не install-manager — той чекає на ADR-027 §Рантайм). Копірайт EN/UK — один рядок, без `{version}`/`{author}`-плейсхолдерів. |

---

## Feature Specs (`features/`)

| File | Title | Status | Summary |
|---|---|---|---|
| `spec-book-covers.md` | Book Covers — System Design | Draft | BookCoverView + BookCoverData; all 66 books; toggle in Settings; Doré image asset strategy |
| `spec-highlights-bookmarks-notes.md` | Highlights, Bookmarks, Notes — Update | Draft | P0/P1/P2 requirements for unified annotation system |
| `spec-localization-i18n.md` | UI Localization & i18n | Draft | EN+UK MVP; MorphologyDecoder abstraction; Settings language picker; BDB translation deferred to P2 |
| `plan-localization-i18n.md` | Localization — Implementation Plan | Draft | 8-step implementation order; LocalizedBundle swizzle; MorphKey extraction; full string audit |
| `plan-highlights-bookmarks-notes.md` | Highlights, Bookmarks, Notes — Implementation Plan | Draft | Step-by-step implementation plan for annotation system |
| `verse_versification_system_design.md` | VersificationService — Design & Spec | Draft | Extract versification logic from ViewModel into VersificationService; consumers: loadWordsForSelectedVerse (done), concordance display (bug), cross-refs |
| `unified_word_lookup_system_design.md` | Unified Word Lookup — Design & Spec | Draft | Unify Оригінал tap + translation long-press into single flow; BibleWord bridge in tapWord (TODO); isParticleSegment non-clickable particles (TODO) |
| `spec-original-nasb-bridge.md` | Original Pill ↔ NASB Bridge | Draft | Supersedes unified_word_lookup; morph-based particle detection; unified word page; chevron nav over non-particle words; verse text highlight from both entry points |
| `spec-word-usage-redesign.md` | Word Usage Tab — Redesign | Draft | Show total count + per-book breakdown (book name · count · one example verse); replaces capped 30-item flat list | **Amendment 2026-08-02:** перехід із рядка книги на вірш-приклад прибрано (без афордансу, стирав cross-ref back-stack, не те очікування) → P2 «всі входження» = stacked sheet зі списком+фільтрами в стилі Пошуку (N буває і 500).
| `spec-trailing-chars.md` | Trailing Characters (maqaf etc.) | Draft | Add `after_char` DB column from Macula XML `after` attr; surface in WordRow; migration script + full rebuild path |
| `spec-verse-sharing.md` | Verse Sharing | Draft | iOS native ShareLink; VerseShareFormatter static func; plain text format with reference + translation id; P0 bottom sheet, P1 context menu |
| `commentary-system-design.md` | Commentary System — Bug Diagnoses & Architecture | Draft | Henry verse→section mapping bug (Genesis 2:7); Owen Hebrews 1 blank screen; range header display; validation checklist |
| `plan-commentary-prerelease-fixes.md` | Виокремлення commentary-БД + фікси джерел — pre-release план (без OTA) | Planned (2026-09-01) | Реалізує Кроки 1-2 ADR-027 (canonical-схема + provenance, org-anchored через `verse_org`) як bundled DB-виокремлення з монолітного `sourcebible.db` — БЕЗ OTA-частини (Кроки 3-4: CDN/manifest/докачування, відкладено до появи немонолітної мови коментарів). Контент-фікси за `commentary-sources-audit.md`: Кальвін (6 сміттєвих рядків геть), Генрі (17 книг/236 глав порожні — патч live-парсера vs перехід на New+ручний патч Луки 16), Оуен (додати вступ+13 оглядів по главі; New-варіант потребує патчу Євр. 1:1-2), Сперджен (додати «Verse Expositions» як ДРУГИЙ твір, не заміну Treasury of David), Едвардс (новий автор, новий MyBible-імпортер) |
| `plan-word-usage-redesign.md` | Word Usage Tab — Implementation Plan | Draft | 7-step order: model → DB method → ViewModel → MorphKey → strings → View; N per-book queries, chapter*1000+verse encoding, stays on MainActor |
| `plan-uk-interlinear-glosses.md` | Український послівний підрядник + інструмент вичитки | **Draft** (фаза 2 пайплайну) | **ПЕРЕМІРЯНО 2026-08-05: одиниця = display-слот, не токен.** `ref` у Macula = `PSA 51:7!5`, де `!5` — слот; кілька токенів = ОДНЕ слово (`VerseTabContent.displayWords`, head = останній не-енклітичний). 470 537 токенів → **305 349 слотів**; 46.4% слотів багатотокенні = 65.2% токенів. … → **[повний запис](INDEX-amendments.md#plan-uk-interlinear-glosses)** |
| `plan-textual-witness-notes.md` | Примітка про рукописне свідчення | Planned (віха не призначена) | Робить вибір базового тексту видимим замість мовчазного. Три ситуації однієї сімʼї: вірша немає в оригіналі (15 віршів TR → ux-021), текст є але спірний (Мк 16:9-20, Мк 16:99 коротке закінчення, Ів 7:53–8:11 — усі ТРИ в Macula є), оригінал є а шляху немає (сироти: `MRK 16:99`, `1CH 12:4` — у корпусі рівно дві). **Не в таблицю `footnote`:** там RST 109 / UBIO 1203 / англійські НУЛЬ, і покриття спірних місць нульове — це апарат перекладу, а примітка стосується тексту. Потрібна курована `textual_note` за координатами оригіналу. Доказовий сид дає `audit_versification.py` (MAT 21:29, PHP 1:16↔17, JHN 16:16, LUK 20:30) |
| `plan-two-originals-vrs.md` | Два оригінали для віршів-склейок (перекваліфіковано з bug-050) | Planned (віха не призначена) | Перехід джерела версифікації з JSON Copenhagen на `.vrs` Paratext (MIT): JSON втрачає повторений ключ, тож із «один→два» виживає один таргет. **19 реальних злиттів** (не 22: три — бойлерплейт `verse 0` = слот надписання, якого в Macula не існує). Потребує `mapping`→список + per-chapter guard за `max_verses` (`rso.vrs` розходиться з нашим RST у 7 главах, з UBIO — у 77). Аудит 2026-08-26 підтвердив мотивацію на 4 псалмах KJV (18:1, 59:1, 60:1, 88:1: перетин 0.14–0.31, разом 0.67–1.00). **Не дефект:** правильність мапінгу 98–99%, див. `scripts/audit_versification.py` |
| `plan-hebrew-translit-rebuild.md` | Hebrew Transliteration — Full DB Rebuild Plan | Approved | New `slot`, `after_char`, `xlit_slot` columns; `fetch_biblehub_translit_hebrew.py` positional scraper; full `build_db.py` rebuild; supersedes `add_after_char.py` migration |
| `spec-study-mode-redesign.md` | Study Mode Redesign | Draft | Verse pins 16pt below toolbar, reader scroll locked; single dynamic-height sheet anchored under verse; toolbar `<>` repurposed to verse/word nav; book/translation picker morphs to Back button (iOS 26); sheet chevrons + bottom action bar → context menu; highlight palette Purple/Pink/Orange/Mint/Blue+None. Includes Fable autonomous run brief |
| `proposal_smarter_examples_v1.5.md` | Smarter Example Selection — v1.5 Proposal | Proposal | Pre-compute `rhetorical_weight` in DB (cross_reference votes); Swift picks highest-weight verse per book instead of first occurrence; fallback chain: rhetorical → syntactic centrality → first |
| `plan-status-bar-cover-white.md` | White Status Bar over Book Cover — algorithm + history | Deferred (working code preserved) | Full code (4 files + ReaderView/ContentView hooks) for the swizzle solution that worked, the variants tried (window-root/class-swizzle/per-instance/scrim) and results, and the ONE open problem: immediate tab-switch (shared HC class + async race; Apple Music sidesteps it via per-screen UIKit VCs). … → **[повний запис](INDEX-amendments.md#plan-status-bar-cover-white)** |
| `plan-semantic-search-rag.md` | Semantic Search + On-Device RAG | Proposal (V1.5) | Apple-native on-device stack: `NLContextualEmbedding` (embed) + `sqlite-vec` (store) + Foundation Models (RAG generation, iOS 26 за `#available`, fallback до semantic search на iOS 18). **Амендить ADR-008** V1.5 upgrade path (свіч embedder ONNX→Apple, +додає generation шар). … → **[повний запис](INDEX-amendments.md#plan-semantic-search-rag)** |
| `spec-windowed-verse-rendering.md` | Windowed Verse Rendering (Doom-style) for Fast Verse Open | Draft / Proposed (fallback) | Render a window `[focus−2 … focus+12]` around the navigated verse + chapter title instead of the whole chapter; present immediately, assemble the rest on sheet dismiss (scroll is locked in Study Mode). … → **[повний запис](INDEX-amendments.md#spec-windowed-verse-rendering)** |
| `spec-reader-resume-position.md` | Reader Resume Position (last book/chapter/scroll) | Draft | Persist last reading position so app reopens where user left off instead of always Genesis 1:1; `@AppStorage` keys (launchBehavior/lastReadBookId/chapter/verseAnchorId) — NOT GRDB (abstracted via `ReadingPositionStore` for later sync) … → **[повний запис](INDEX-amendments.md#spec-reader-resume-position)** |
| `plan-ios18-compat.md` | iOS 18 Compatibility Refactor — Safe Half (Fable) + Deferred Sprint | Phase A DONE (2026-07-06) · **Phase B: target flipped 2026-07-31, QA відкрите** | Розбиває відкладений compat-спринт (ADR-001) на дві фази. **Phase A (Fable зараз):** додати відсутні `#available(iOS 26)` guard-и + iOS 18 fallback, target ЛИШАЄТЬСЯ 26.4 у фінальному diff (зворотно, без зміни поведінки на iOS 26). Техніка: … → **[повний запис](INDEX-amendments.md#plan-ios18-compat)** |
| `fable-brief-crossref-verse_org.md` | Fable Run-Brief — ALL verse_map consumers → verse_org (ADR-028 Phase 2) | **Done** (виконано; заміряно 2026-08-07: `verse_map` у базі немає, `verse_org` = 155 621) | **3 SQL-споживачі verse_map** (усі биті для UBIO=0 рядків + 57% хибні): `loadConcordance`, `loadBookUsageGroups` (UBIO Псалми Usage → англ. KJV fallback), `loadCrossReferences`/`convertVerse` (чужий вірш+«Text unavailable»). Мігрувати ВСІ на reverse `verse_org` (idx_verse_org_rev), тоді DROP. … → **[повний запис](INDEX-amendments.md#fable-brief-crossref-verse-org)** |
| `fable-brief-adr026-paging.md` | Fable Run-Brief — Reader Chapter Paging (ADR-026 Phase 2) | **Done** (прогін завершено, ADR-026 IMPLEMENTED 2026-07-10) | Autonomous brief for `TabView(.page)` chapter paging. ALLOW: reader container in `ReaderView.swift`, selection binding, chevron+full-surface swipe, current±1 prefetch, retire `EdgeSwipeNavigator`. DENY: reimplement chapter view, Study-Mode logic (ADR-021), DB, ADR-016/024, analytics. Hard rules: … → **[повний запис](INDEX-amendments.md#fable-brief-adr026-paging)** |
| `spec-nt-lemma-form-clickability.md` | NT Greek lemma-vs-form — word clickability gap | **Deferred** | НЗ: Macula тегує ЛЕМУ (σύ G4771), переклади ФОРМУ (ὑμῖν G5213) → займенники/частки перекладу не мапляться на Macula за номером → не-клікабельні в «Оригіналі». Виявлено при ADR-028 O2. … → **[повний запис](INDEX-amendments.md#spec-nt-lemma-form-clickability)** |
| `spec-analytics-mixpanel.md` | Analytics (Mixpanel) Integration | Draft | Mixpanel in beta AND prod (D3 ✅), gated on consent toggle (`analyticsEnabled`, default ON) — NOT `#if BETA`; PreAuthIdentity distinct_id; event taxonomy (engagement/retention, search, feature adoption); per-session `study_session_summary` → MSS as tunable Mixpanel Custom Event … → **[повний запис](INDEX-amendments.md#spec-analytics-mixpanel)** |
| `conventions-uk-interlinear.md` | Конвенції українського підрядника — чернетка v0.1 | Draft (2026-08-05) | Правила глосування ОДНОГО display-слота, виведені емпірично з подвійного проходу по золотому набору. Ядро: (1) слот несе рівно свій матеріал — не позичати в сусіда й не лишати сусідові; (2) **форму дає `morph`, лексичний вибір — англійська глоса** (плутанина цих двох джерел дає кальку в називному) … → **[повний запис](INDEX-amendments.md#conventions-uk-interlinear)** |
| `spec-notifications.md` | Notifications (Phase 1, local-first) | Draft | Реалізує ADR-031. P0: **permission-гібрид** (provisional D0 → soft-ask full після 1-го study-моменту), **канал A** «Продовжити дослідження» (event-driven, снапшот word-study), **канал B** курований вірш (rotating `encourage`/`deep_dive`, no-repeat, front-load) … → **[повний запис](INDEX-amendments.md#spec-notifications)** |
| `spec-donation-community.md` | Donation Community Prompt | Draft | Реалізує ADR-038. Повний EN/UK текст sheet'а «Спільнота SourceBible» / «SourceBible Community» (inline, не xcstrings, за зразком About): місія, 4 підрозділи (контекст Писання, розумний пошук/ШІ, українські ресурси, інструменти служіння), заклик. EN §3 — не переклад, а mission/investment-переписування (не дублює укр.-специфічний список фіч); мовний список/порядок ще не затверджено, іспанська ймовірна перша після укр., **російська свідомо відсутня** — ціннісне рішення (чекаємо кінця війни), не технічне. Розбіжність зі `spec-localization-i18n.md` (RU/ES/DE/PL) щодо мовного ряду — не синхронізовано, маркетинговий текст не архітектурне зобов'язання. |

---

## Product Decisions (`product decisions/`)

| File | Title | Status | Summary |
|---|---|---|---|
| `PDR-Auth-Strategy.md` | Trust-First Authentication | Accepted | No login wall; local-first data; sync opt-in |
| `PDR-Analytics-Mixpanel.md` | Analytics (Mixpanel) | Proposed | Mixpanel in beta AND prod, anonymous (PreAuthIdentity), no PII; gated on consent toggle (default ON), not build-flag; North Star = Meaningful Study Session; metric defs live in Mixpanel (tunable) … → **[повний запис](INDEX-amendments.md#pdr-analytics-mixpanel)** |
| `PDR-Highlights-Bookmarks-Notes.md` | Notes/Highlights/Bookmarks Differentiation | — | Distinct UX roles for three annotation types |
| `PDR-Book-Covers.md` | Book Cover Design — Doré Engravings | Accepted | Doré engraving + blue bg; Protestant canonical section tag + chapter count per cover; section vocab mirrors browse flow |
| `PDR-Hebrew-Transliteration-Rules.md` | Hebrew Transliteration Rules for `simplify_xlit()` | Accepted | Grammar rules + 4 bugs fixed: monosyllabic shewa, dagesh doubling, spirant peh (combining char), modifier letter ᵃ |
| `PDR-Lexicon-Language.md` | Lexicon Language — English glosses/definitions for MVP | Accepted 2026-07-14 | Lexical **data** (Macula glosses, Strong's short/long_def) is English-only for MVP regardless of UI language or translation — dataset is human-authored English, no RST/UA gloss column, MT of definitions rejected on trust grounds. **Interface labels MUST still localize** … → **[повний запис](INDEX-amendments.md#pdr-lexicon-language)** |
| `PDR-Page-Turn-Gesture-Zone.md` | Page-Turn Swipe Zone | Amended 2026-07-10 → full-surface | **NOW full-surface swipe** via `TabView(.page)` (ADR-026), supersedes edge-only. Reversible: **rollback clause** → revert to edge-only (`EdgeSwipeNavigator`) if ADR-026 Phase-2 device check shows real clash with word tap / long-press / selection. Resolves ux-001 discoverability (close ux-001 when Phase 2 lands). Original edge-only rationale kept below the fold as superseded record |

---

## Reference (`/`)

| File | Summary |
|---|---|
| `about-page.md` | **About page copy (EN+UK)** — final marketing/attribution text for Menu → About. «We bridge the gap…», softened «no seminary», manuscripts (Westminster Leningrad Codex / Nestle 1904), tagline «Built by Bible students and teachers, for Bible students and teachers», Ukraine/war + support, sources & licenses (KJV/ASV/RST, Macula, Strong's, TBESH/TBESG, commentaries, OpenBible, Doré). Copy source of truth; implemented in `SourceBible/Views/Menu/AboutView.swift` (Liquid Glass support button → DonationView). No BibleHub translit. **UBIO (Огієнко 1988) додано до списку джерел 2026-07-31** — CC BY-SA 3.0, дозвіл УБТ (ADR-029 розблоковано); NASB лишається пропущеним (ліцензований) |
| `glossary.md` | Term definitions — word-study surfaces (original ≠ lexicon ≠ concordance), analytics taxonomy, session/consent, Mixpanel projects. Read when terms feel ambiguous |
| `ux/known-design-friction.md` | Register of "works as designed but confuses users" cases + discoverability decisions (accept/affordance/tutorial); feeds the onboarding tutorial. Seeded with ux-001 |
| `ux/feature-requests.md` | Backlog of net-new features asked by users, with target milestone (board status `planned`). ux-017 → v1.5; candidates ux-005/011/016 |
| `original-tab.md` | **Вкладка «Оригінал» від Macula TSV до екрана** — читати ПЕРЕД будь-якою задачею про оригінальні мови, глоси, транслітерацію, слоти чи морфологію. Існує щоб агенти не переоткривали вирішене. **Одиниця відображення = слот (`word.slot`), НЕ рядок `word`**: групування в `VerseTabContent.displayWords:280` (а не в `WordTabContent` — назва обманює); 46.4% івритських слотів багатотокенні = 65.2% токенів; head = **останній не-енклітичний** токен (`headToken:269`), стара евристика «перший не-helper» помилялась на 8.5%. **Три різні колонки глос:** `word.gloss` = TSV `english` (по-токенна), `gloss_macula` = XML lowfat `gloss` (**фраза рівня слота, порізана по токенах** — тому дієслівний токен несе `she`, а суфікс `conceived.me`; це не баг, а властивість колонки), `gloss_display` = синтез `process_glosses.py` (6 стадій: знімає крапковий префікс займенника, розвертає конструктний ланцюжок, крапки→пробіли). Показується `COALESCE(gloss_display, gloss_macula, gloss)`. Транслітерація: `bestXlit = xlitSlot ?? xlit ?? xlitSimple`, у лексиконного хедера **інший** ланцюжок (лема, не форма). Версифікація ADR-028 — три випадки: рядка немає → identity, `org_*` NULL → `[]` (чесний порожній стан), N:M → конкатенація всіх рефів. `loadStrongs` fallback на базовий Strong's лише при збігу першого символу (H871≠H871a, H835=H835a). Кешу слів і префетчу немає; усе синхронно під `@MainActor`. **Заміри корпусу:** 470 537 токенів / 305 349 слотів / 63 487 токенних трійок / 78 451 слотових одиниць; 80% покриття = 5 584 токенні трійки або 21 874 слотові. **Розбіжності:** `verify_xlit_integrity` НЕ падає, хоч docstring обіцяє `SystemExit`; `build_db.py` пише `xlit_slot` на провідний прийменник, а погляд компенсує читанням `compactMap(\.xlitSlot).first` |
| `BUILD.md` | **Нова машина — від `git clone` до запущеного застосунку.** Що є в клоні, а чого немає: `sourcebible.db` (340 MB ×2) і `data/` (1.6 GB) поза git, тож із самого клону застосунок НЕ зібрати. Два шляхи до бази — скопіювати з іншої машини (за замовчуванням, детерміновано) або перезібрати через `./rebuild.sh` (тільки якщо міняєте конвеєр). Xcode обов'язковий (CLion/VS Code не збирають iOS), Python ≥3.10 з python.org, секрети з шаблону `Secrets.xcconfig`. Перелік датасетів НЕ дублює — він у `db_build.md`; процедуру збірки НЕ дублює — вона в `rebuild.sh`. Заведено 2026-08-08: `.gitignore` посилався на цей файл двічі, а файлу не існувало |
| `db_build.md` | Збірка `sourcebible.db`: походження й **ліцензії датасетів** (єдине місце), схеми `word`/`strongs`, реєстр відомих пасток із кодом виправлення. **Процедура тут НЕ живе — канонічний шлях `./rebuild.sh`**, документ лише перелічує 6 кроків для орієнтації. Оновлено 2026-08-05: `verse_map`/`build_verse_map.py`/`findBestMaculaVerse` викинуто (ADR-028 фаза 2) — лишились тільки як ⛔ «не відроджувати»; додано крок `process_glosses.py` (без нього крапки в UI) і **три колонки глос**: `gloss` = TSV `english` по-токенна, `gloss_macula` = XML фраза рівня слота, `gloss_display` = синтез і те, що видно; схема `word` доведена до 20 колонок (`slot`, `xlit_slot`, `after_char`, `lexical_class`, `gloss_display`). Розділ «Історія розходження»: `rebuild.sh` полагодили 16.07 о 12:25, документ чіпали о 15:10 і не помітили — звідси правило, що процедура належить скрипту. ⚠️ Зафіксовано: `verify_xlit_integrity` НЕ падає, хоч docstring обіцяє `SystemExit`. Runtime вкладки «Оригінал» — `original-tab.md` |
| `uk-interlinear-program.md` | **Український підрядник — програма, знахідки, правила. Читати ПЕРЕД будь-якою задачею про укр. глоси, розмітку Огієнка по Стронгу чи волонтерську вичитку.** Три епіки з обґрунтованим порядком: **1) підрядник → 2) розмітка Огієнка (`verse_markup`, ADR-016 Phase 2) → 3) волонтерський сервіс** — Огієнко приходить без Стронга (`strong_numbers=false`, ADR-029), тож підрядник будує саме ту вирівняну пару «слот івриту → укр. глоса», без якої розмітка є вирівнюванням з нуля; конвенції та сторінка вичитки — теж продукт епіка 1, тому волонтери йдуть після. **Одиниця роботи = display-слот**, обсяг 80% покриття = **21 874 слотові одиниці** (старе 8 951 не відтворюється — не переносити). Знімок виміру 2026-08-05: гілка A 65% проти Gemma 31B 52%; предиктор складності — кількість морфем у слоті (1→79/70%, 2→48/35%, 3→50/0%), не ярус частотності; жанр сильніший за ярус (закон 100/38%, поезія 38/25%); **18 одиниць мимо в обох, 10 з однаковою відповіддю = невимовлені конвенції, не якість**. Людина: сліпе писання 22.8 с/од., вичитка 4.1 с/од. → 138 год проти 25–38 год. Токени ≈$8 (80%) / ≈$30 (весь корпус) на Opus batch — не обмеження. Таблиця антипатернів (слот≠токен, три колонки глос, NULL vs порожній рядок, substring-леак-чеки, контроль на контроль, хардкод проти файлу, фікстури з реального корпусу). ⚠️ `pilot_gold_filled_v3.tsv` — **заморожений** знімок: набір забруднений, чисте число дасть лише новий сліпий прохід на 149 одиницях, що лишились |
| `xlit_subentry_system_design.md` | Transliteration sub-entry handling (H871a, H1886d, etc.) — implemented |
| `verse_offset_system_design.md` | Verse offset/numbering system design — implemented (findBestMaculaVerse, Strong's overlap) |
| `bsb-publishing-dataset-analysis.md` | BSB/Berean (BSB-publishing) datasets — CC0 text, STEPBible TBESH/TBESG lexicon, UBS versification; BSB won't dissolve NASB↔Macula semantic mismatch (same BibleHub lineage); two paths (BSB-as-clickable-translation vs override auto-gen); 3 next steps open |
| `strongs-mismatch-analysis.md` | Empirical NASB/BibleHub↔Macula Strong's mismatch (whole OT), FOR the longtap gate (ADR-016) + BSB decision. 3 types: (1) semantic noun/verb — unfixable by any table (H341/H340 "enemy" 280×); (2) namespace H9000+ — fixable, = `NASBExtendedOverride`/eStrong; (3) name-variants — fixable via uStrong. Evaluates `mStrong` canonical key = key column of ADR-016 Phase 2 `verse_markup`; recommend Macula-anchored, seeded from STEPBible dStrong/uStrong → future ADR-028 |
| `commentary-sources-audit.md` | **Ревізія `data/` vs `data/New/` для коментарів (2026-08-28)** — читати перед будь-якою роботою над імпортом commentary-джерел (ADR-027 пілот). Кальвін: підтверджено злиття (Буття 10-30 патч, Ісая 49-66 повна в MyBible без потреби SWORD-splice), 6 сміттєвих рядків. Генрі: **живий парсер повертає порожньо для 236/1189 глав (19.8%, 17 повних книг)** — два несумісні HTML-формати розмітки в джерелі; нове MyBible-джерело розбирає обидва, але має 1 ізольований дефект (Луки 16:19-31 загублено). Оуен: поточний імпортер ніколи не читає вступ+13 оглядів по главі (14 розділів); нове джерело їх має, але губить Євр. 1:1-2. Сперджен: нове джерело («Verse Expositions») — **інший твір**, не заміна Treasury of David. Августин: є, але російською, вибіркова антологія. Бонус: Едвардс (новий автор), рос. сиблінги Генрі/Кальвіна |

## Product Docs (`BRD/`)

> ⚠️ **These docs reflect the pre-SwiftUI React Native prototype. Rewrite before release to reflect the current SwiftUI + iOS 26 stack.**

| File | Summary |
|---|---|
| `01-context.md` | Project context and background |
| `02-metrics.md` | Success metrics and KPIs |
| `03-brd.md` | Business Requirements Document (outdated — describes React Native + Expo) |
| `04-user-flows.md` | User flows (Markdown) |
| `05-architecture-diagram.md` | High-level architecture diagram (outdated) |
| `07-potential-team.md` | Team structure and roles |

---

## Трекери зворотного зв'язку (`bugs/`, `ux/`)

Два паралельні конвеєри з тестерського мейлбоксу. Маршрутизацію робить скіл `process-bugs`:
**зламане** → `bugs/`, **тертя або побажання** → `ux/`. UX-елементи ніколи не дедуплікуються
проти дефектів. Життєвий цикл тек — у `ux/README.md`.

| File | Summary |
|---|---|
| `bugs/TEMPLATE.md` | Схема одного дефекта: `id/type/status/severity/priority/**area** (ключ дедупу)/component/reporters/app_version/device/ios_version/context/date_reported/duplicate_of/related/source`. Теки: `new` → `in-progress` / `blocked` → `done`; `claude/` — розслідування, `img/` — скріншоти |
| `bugs/processed-sprints.md` | Реєстр оброблених спринтів мейлбоксу — ключ ідемпотентності, щоб те саме не втягувалось двічі |
| `ux/README.md` | Конвеєр UX: `new` → `reviewed` (проставлено impact + effort) → `done`; пріоритезація impact × effort, кілька репортерів піднімають impact |
| `ux/TEMPLATE.md` | Схема одного UX-елемента |
| `ux/known-design-friction.md` | Реєстр «працює як задумано, але плутає» + рішення про дискаверабіліті (accept / афорданс / туторіал). Живить онбординг |
| `ux/feature-requests.md` | Беклог net-new фіч від користувачів із цільовою віхою |

---

## Система пам'яті (`/`)

| File | Summary |
|---|---|
| `WIKI.md` | **Протокол пам'яті (LLM Wiki)** — операції ingest/query/lint, 3 шари (джерела/wiki/schema), конвенції. «Schema»-шар бази знань |
| `LOG.md` | **Append-only хронологія** рішень/ingest/lint — хребет пам'яті («що і коли»). Формат `## [дата] тип \| заголовок` |
| `INDEX-amendments.md` | Довгі історії амендментів, винесені з цього файлу 2026-08-07 — щоб INDEX лишався картою, а не архівом |
| `project-memory.md` | Снапшот стану для knowledge claude.ai-Проєкту (Cowork не читає `CLAUDE.md` автоматично). Оновлювати разом із lint-ом |
| `../scripts/lint_docs.py` | **Лінтер пам'яті** — звіряє шапки ADR/spec із рядками цього файлу, ловить сироти, биті посилання і дні з трейлером `Decision:` без запису в LOG. Падає ненульовим кодом. Ручна звірка провалилась 7 разів із 34 |
| `../scripts/lint_localization.py` | **Лінтер узгодження числа й іменника** (bug-041) — plural-варіації в `Localizable.xcstrings`: числовий формат біля іменника без plural, неповний набір категорій uk (one/few/many/other), plural-ключ, форматований без `locale:` (бере СИСТЕМНУ мову — саме це дало «3 розділу»), тернар однина/множина навколо `NSLocalizedString`. Ненульовий код виходу |
| `../scripts/pack_untracked.py` | **Пакувальник переїзду** — zip усього, чого немає в git (2.37 GB у 33 794 файлах). Перелік бере з `git ls-files --others --ignored`, не з хардкоду. Профілі `db` (340 MB) / `essential` (1.1 GB) / `full` (1.9 GB); типово друкує лише план із причинами пропуску. Кладе `MANIFEST.json` (sha256 бази, коміт-джерело) і виконуваний `restore.sh`. ⚠️ містить живі токени Mixpanel. Див. `BUILD.md` |
| `../scripts/harvest_log.py` | **Збирач чернетки LOG** із git-історії: трейлери `Decision:`/`Refs:`, закриті баги (перейменування в `bugs/done/` — одиниця ДАТА, не спринт), плюс кандидати за маркерами відкинутої альтернативи («а не», «замість», «відкочено»). Друкує чернетку; `--write` вставляє в LOG на вичитку |

---

## Архів (не читати без потреби)

`docs-audit-2026-05-26.md` (перший аудит доків), `weekly-review-2026-06-05.md`,
`post-pull-patches.md`, `board.html`, `design-system.html`, `tbesh_test_results.log` —
історичні знімки, не джерела правди.
